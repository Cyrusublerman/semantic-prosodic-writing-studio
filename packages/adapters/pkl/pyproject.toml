[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spws-pkl-adapter"
version = "0.1.0"
description = "SPWS PKL snapshot indexer and retriever"
requires-python = ">=3.12,<3.15"
dependencies = [
  "spws-contracts-core",
  "pyyaml>=6.0",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
