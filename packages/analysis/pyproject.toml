[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spws-analysis"
version = "0.1.0"
description = "SPWS poetry analysis with optional WordRare integration"
requires-python = ">=3.12,<3.15"
dependencies = [
  "spws-contracts-core",
  "spws-domain",
  "spws-semantics",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
