[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spws-domain"
version = "0.1.0"
description = "SPWS domain helpers for ids, spans, and source invariants"
requires-python = ">=3.12,<3.15"
dependencies = [
  "spws-contracts-core",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
