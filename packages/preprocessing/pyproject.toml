[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spws-preprocessing"
version = "0.1.0"
description = "SPWS source normalisation and fragment proposal catalogue"
requires-python = ">=3.12,<3.15"
dependencies = [
  "spws-contracts-core",
  "spws-domain",
  "spws-semantics",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
